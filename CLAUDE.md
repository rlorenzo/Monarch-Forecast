Project conventions for AI coding assistants live in [AGENTS.md](AGENTS.md).

This file exists so Claude Code, which auto-loads `CLAUDE.md` by
convention, picks them up too. `AGENTS.md` is the cross-tool standard
(Cursor, Codex, Antigravity); keep it as the source of truth.
